---
import LeadForm from './LeadForm.astro';
import YouTubeFacade from './YouTubeFacade.astro';

const results = [
  { before: '001A-1.jpg', after: '001B-1.jpg', label: 'Caso 01 · línea frontal' },
  { before: '002A.jpg', after: '002B.jpg', label: 'Caso 02 · densidad superior' },
  { before: '006A.jpg', after: '006B.jpg', label: 'Caso 03 · entradas' },
] as const;

const faqs = [
  {
    question: '¿Cómo sé si soy candidato a un implante capilar?',
    answer:
      'La evaluación revisa tu tipo de alopecia, la calidad de la zona donante, tus antecedentes y el resultado que buscas. Con esa información el equipo puede indicarte si el implante es adecuado para ti.',
  },
  {
    question: '¿Cómo sé si necesito FUE o FUSS?',
    answer:
      'No tienes que decidirlo antes. El equipo evalúa tu tipo de alopecia, zona donante, cantidad de folículos y objetivos para recomendar FUE, FUSS o una combinación de ambas.',
  },
  {
    question: '¿Es necesario internarse?',
    answer:
      'No. Es una cirugía ambulatoria con anestesia local. Recibes indicaciones de cuidado y continúas con el programa de seguimiento de la clínica.',
  },
  {
    question: '¿El cabello implantado es permanente?',
    answer:
      'Los folículos se obtienen de una zona genéticamente más resistente a la alopecia. Su permanencia y la evolución del cabello nativo se revisan en la evaluación y en los controles posteriores.',
  },
  {
    question: '¿Cómo es la recuperación?',
    answer:
      'Recibirás indicaciones específicas para los primeros días y controles médicos durante la evolución. Los tiempos dependen de la técnica y de las características de cada paciente.',
  },
  {
    question: '¿El procedimiento duele?',
    answer:
      'La cirugía se realiza con anestesia local. Durante la evaluación el equipo explica cada etapa, las sensaciones esperables y los cuidados para una recuperación confortable.',
  },
] as const;
---

<div class="version2">
  <header class="v2-nav" data-v2-nav>
    <div class="v2-container v2-nav__inner">
      <a class="v2-nav__brand" href="#inicio" aria-label="Clínica Témpora, inicio">
        <img src="/assets/img/logo-tempora-blanco.webp" width="321" height="54" alt="Clínica Témpora" />
      </a>
      <nav class="v2-nav__links" aria-label="Navegación principal">
        <a href="#resultados">Resultados</a>
        <a href="#proceso">Proceso</a>
        <a href="#tecnicas">Técnicas</a>
        <a href="#preguntas">Preguntas</a>
      </nav>
      <a class="v2-button v2-button--lime v2-nav__cta" href="#form">Quiero agendar</a>
    </div>
  </header>

  <main id="hero">
    <section class="v2-hero" id="inicio">
      <div class="v2-hero__glow" aria-hidden="true"></div>
      <div class="v2-container v2-hero__grid">
        <div class="v2-hero__message">
          <div class="v2-hero__copy">
            <p class="v2-kicker">Clínica especializada en restauración capilar</p>
            <h1>Implante capilar diseñado para <span>verse natural.</span></h1>
            <ul class="v2-hero__checks" aria-label="Características principales">
              <li><span aria-hidden="true">✓</span> FUE, FUSS o una combinación según tu diagnóstico</li>
              <li><span aria-hidden="true">✓</span> Cirugía ambulatoria con seguimiento médico</li>
              <li><span aria-hidden="true">✓</span> Evaluación presencial o a distancia</li>
            </ul>
            <div class="v2-hero__actions">
              <a class="v2-button v2-button--lime" href="#form">Quiero agendar</a>
              <a class="v2-text-link" href="#resultados">Ver resultados <span aria-hidden="true">↘</span></a>
            </div>
          </div>
          <figure class="v2-hero__visual">
            <img src="/assets/img/v2/tempora-galeria4.webp" width="727" height="823" alt="Especialista de Clínica Témpora realizando un implante capilar" />
            <figcaption><strong>Planificación médica</strong><span>Diseño, técnica y seguimiento para cada caso.</span></figcaption>
          </figure>
        </div>

        <div class="v2-form-shell" id="form" data-form-shell>
          <div class="v2-form-shell__topline">
            <span>Evaluación capilar</span>
            <span>2 min aprox.</span>
          </div>
          <LeadForm fieldsPerStep={4} />
        </div>
      </div>
    </section>

    <section class="v2-results v2-section" id="resultados">
      <div class="v2-container">
        <div class="v2-heading v2-heading--center v2-heading--light">
          <p class="v2-kicker">Resultados reales de Clínica Témpora</p>
          <h2>Una línea capilar no se copia.<br /><span>Se diseña para cada persona.</span></h2>
          <p>Casos publicados por la clínica. El resultado y la evolución pueden variar entre pacientes.</p>
        </div>

        <div class="v2-results__grid">
          {results.map((result, index) => (
            <article class:list={['v2-case', { 'v2-case--wide': index === 0 }]}>
              <div class="v2-case__images">
                <figure>
                  <img src={`/assets/img/v2/${result.before}`} width="1080" height="872" alt={`${result.label}, antes del implante capilar`} loading="lazy" />
                  <figcaption>Antes</figcaption>
                </figure>
                <figure>
                  <img src={`/assets/img/v2/${result.after}`} width="1080" height="872" alt={`${result.label}, después del implante capilar`} loading="lazy" />
                  <figcaption>Después</figcaption>
                </figure>
              </div>
              <h3>{result.label}</h3>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="v2-method v2-section" id="proceso">
      <div class="v2-container v2-method__grid">
        <div class="v2-method__copy">
          <p class="v2-kicker v2-kicker--dark">Un plan médico, no una solución genérica</p>
          <h2>No se trata de “poner pelo”. Se trata de decidir bien dónde, cuánto y cómo.</h2>
          <p class="v2-method__lead">
            El diseño, la zona donante y la técnica determinan la naturalidad del resultado. Por eso el proceso comienza con un diagnóstico y termina con seguimiento.
          </p>

          <ol class="v2-steps">
            <li><span>01</span><div><strong>Evaluación</strong><p>Revisamos alopecia, zona donante, objetivos y antecedentes.</p></div></li>
            <li><span>02</span><div><strong>Diseño</strong><p>Definimos la línea capilar y la cantidad estimada de folículos.</p></div></li>
            <li><span>03</span><div><strong>Cirugía</strong><p>Aplicamos FUE, FUSS o ambas según la indicación médica.</p></div></li>
            <li><span>04</span><div><strong>Seguimiento</strong><p>Acompañamos la evolución con controles postoperatorios.</p></div></li>
          </ol>
        </div>

        <div class="v2-method__gallery" aria-label="Proceso clínico en Clínica Témpora">
          <img class="v2-method__gallery-main" src="/assets/img/v2/hero-foto.webp" width="984" height="711" alt="Procedimiento de implante capilar en Clínica Témpora" loading="lazy" />
          <img src="/assets/img/v2/tempora-galeria3.webp" width="868" height="859" alt="Evaluación médica personalizada en Clínica Témpora" loading="lazy" />
          <img src="/assets/img/v2/tempora-galeria6.webp" width="920" height="797" alt="Preparación de paciente para cirugía capilar" loading="lazy" />
        </div>
      </div>
      <div class="v2-container v2-tech" id="tecnicas">
        <div class="v2-tech__intro">
          <p class="v2-kicker v2-kicker--dark">Indicación médica personalizada</p>
          <h3>Trabajamos con FUE y FUSS. El equipo define cuál se adapta a tu caso.</h3>
          <p>La elección depende de tu zona donante, el grado de alopecia y la cantidad de folículos necesarios.</p>
        </div>
        <div class="v2-tech__cards">
          <article>
            <span>Extracción individual</span>
            <h3>FUE</h3>
            <p>Extrae las unidades foliculares una a una, sin cicatriz lineal.</p>
            <ul><li>Recuperación más rápida</li><li>Permite llevar el cabello corto</li></ul>
          </article>
          <article>
            <span>Máximo aprovechamiento donante</span>
            <h3>FUSS</h3>
            <p>Obtiene una banda de tejido que se separa bajo microscopio.</p>
            <ul><li>Mayor volumen de injertos</li><li>Indicada en casos seleccionados</li></ul>
          </article>
        </div>
      </div>
    </section>

    <section class="v2-media v2-section" id="historias">
      <div class="v2-container">
        <div class="v2-heading v2-heading--split v2-heading--light">
          <div>
            <p class="v2-kicker">Casos, no promesas</p>
            <h2>La experiencia contada por quienes la vivieron.</h2>
          </div>
          <p class="v2-heading__aside">Pacientes y medios han documentado el proceso, la evolución y el acompañamiento de Clínica Témpora.</p>
        </div>

        <div class="v2-media__carousel" data-media-carousel>
          <div class="v2-media__track" data-media-track>
            <article class="v2-media__slide is-active" data-media-slide>
              <div class="v2-media__video"><YouTubeFacade id="3YyPCaqcIXQ" alt="Reportaje de 24 Horas sobre un implante capilar" thumb="/assets/img/v2/video-reportaje-paciente.webp" /></div>
              <div class="v2-media__copy"><span>Reportaje 24 Horas</span><h3>Un caso real seguido durante un año.</h3><p>El proceso completo, desde la evaluación hasta la evolución del resultado.</p></div>
            </article>
            <article class="v2-media__slide" data-media-slide>
              <div class="v2-media__video"><YouTubeFacade id="Oth49NiG8Y0" alt="Paciente contando su experiencia con el implante capilar" thumb="/assets/img/v2/JOAQUIN.jpg" /></div>
              <div class="v2-media__copy"><span>Testimonio de paciente</span><h3>La experiencia en primera persona.</h3><p>Un paciente relata su decisión, el procedimiento y el acompañamiento recibido.</p></div>
            </article>
            <article class="v2-media__slide" data-media-slide>
              <div class="v2-media__video"><YouTubeFacade id="ybZsii5iUIE" alt="Procedimiento de implante capilar FUE" thumb="/assets/img/v2/video-procedimiento.webp" /></div>
              <div class="v2-media__copy"><span>Proceso clínico</span><h3>Así se realiza un implante FUE.</h3><p>Una mirada al trabajo médico dentro del pabellón.</p></div>
            </article>
          </div>
          <div class="v2-media__controls">
            <button type="button" aria-label="Video anterior" data-media-prev>←</button>
            <div class="v2-media__dots" aria-label="Seleccionar video">
              <button type="button" class="is-active" aria-label="Ver video 1" aria-current="true" data-media-dot="0"></button>
              <button type="button" aria-label="Ver video 2" data-media-dot="1"></button>
              <button type="button" aria-label="Ver video 3" data-media-dot="2"></button>
            </div>
            <button type="button" aria-label="Video siguiente" data-media-next>→</button>
          </div>
        </div>
      </div>
    </section>

    <section class="v2-clinic v2-section" id="clinica">
      <div class="v2-container v2-clinic__grid">
        <div class="v2-clinic__visual">
          <img src="/assets/img/v2/JESUS-1_00000000-2.jpg" width="600" height="600" alt="Dra. Jesús Díaz, fundadora y directora médica de Clínica Témpora" loading="lazy" />
          <img src="/assets/img/v2/FRANCISCO-1_00000000.jpg" width="600" height="600" alt="Dr. Francisco Castellón, director de restauración capilar" loading="lazy" />
        </div>
        <div class="v2-clinic__copy">
          <p class="v2-kicker v2-kicker--dark">Equipo médico especializado</p>
          <h2>Tu resultado comienza con quién evalúa y opera.</h2>
          <p>
            Clínica Témpora reúne profesionales dedicados a restauración capilar FUE y FUSS, con formación internacional y actualización continua.
          </p>
          <div class="v2-clinic__doctors">
            <div><strong>Dra. Jesús Díaz</strong><span>Fundadora y directora médica</span></div>
            <div><strong>Dr. Francisco Castellón</strong><span>Director de restauración capilar</span></div>
          </div>
        </div>
      </div>
    </section>

    <section class="v2-faq v2-section" id="preguntas">
      <div class="v2-container v2-faq__grid">
        <div class="v2-faq__intro">
          <p class="v2-kicker v2-kicker--dark">Antes de decidir</p>
          <h2>Preguntas que merecen respuestas claras.</h2>
          <img src="/assets/img/v2/PABLO.jpg" width="750" height="750" alt="Paciente de Clínica Témpora durante su testimonio" loading="lazy" />
        </div>
        <div class="v2-faq__list">
          {faqs.map((faq) => (
            <details>
              <summary>{faq.question}<span aria-hidden="true">+</span></summary>
              <p>{faq.answer}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <section class="v2-final">
      <div class="v2-container v2-final__panel">
        <img src="/assets/img/v2/JULIANO.jpg" width="750" height="750" alt="Paciente de Clínica Témpora después de su tratamiento" loading="lazy" />
        <div class="v2-final__copy">
          <p class="v2-kicker">Tu caso empieza con una evaluación</p>
          <h2>Descubre qué resultado es posible para ti.</h2>
          <a class="v2-button v2-button--lime" href="#form">Quiero agendar</a>
        </div>
      </div>
    </section>
  </main>

  <footer class="v2-footer">
    <div class="v2-container v2-footer__inner">
      <img src="/assets/img/logo-tempora-blanco.webp" width="321" height="54" alt="Clínica Témpora" />
      <div><strong>Suecia 0142, oficina 603</strong><span>Providencia, Santiago</span></div>
      <div><strong>Lunes a viernes</strong><span>9:00 a 18:00 hrs.</span></div>
    </div>
  </footer>

  <a class="v2-floating" href="#form" data-floating>Quiero agendar <span aria-hidden="true">→</span></a>
</div>

<style is:global>
  @font-face { font-family: 'Manrope'; src: url('/assets/fonts/manrope-latin.woff2') format('woff2'); font-style: normal; font-weight: 400 800; font-display: swap; }
  html { scroll-padding-top: 76px; }
  body > .skip-link { display: none; }
  .version2 { --v2-ink: #061a1c; --v2-ink-2: #0d2a2d; --v2-cream: #f5f4e9; --v2-paper: #fffef8; --v2-lime: #bad305; --v2-mint: #dfe8dc; --v2-muted: #53686a; --font-display: 'Manrope', system-ui, sans-serif; --font-body: 'Manrope', system-ui, sans-serif; background: var(--v2-cream); color: var(--v2-ink); font-family: var(--font-body); overflow: clip; }
  .version2 [hidden] { display: none !important; }
  .version2 *, .version2 *::before, .version2 *::after { box-sizing: border-box; }
  .version2 h1, .version2 h2, .version2 h3 { font-family: var(--font-display); letter-spacing: -0.035em; }
  .version2 h2 { font-size: clamp(2.25rem, 3.7vw, 3.8rem); line-height: 1; font-weight: 750; }
  .version2 p { font-family: var(--font-body); }
  .v2-container { width: min(1240px, calc(100% - 48px)); margin-inline: auto; }
  .v2-section { padding: clamp(62px, 6.5vw, 94px) 0; }
  .v2-kicker { margin: 0 0 16px; color: var(--v2-lime); font: 750 .72rem/1 var(--font-body); letter-spacing: .15em; text-transform: uppercase; }
  .v2-kicker--dark { color: #668000; }
  .v2-button { display: inline-flex; min-height: 48px; align-items: center; justify-content: center; padding: 13px 22px; border-radius: 999px; font: 700 .94rem/1 var(--font-body); transition: transform 180ms ease, background 180ms ease, color 180ms ease; }
  .v2-button:hover { transform: translateY(-2px); }
  .v2-button--lime { background: var(--v2-lime); color: var(--v2-ink); }
  .v2-button--dark { background: var(--v2-ink); color: white; }
  .v2-heading { margin-bottom: 42px; }
  .v2-heading h2 span { color: #779400; }
  .v2-heading--light h2, .v2-heading--light h3 { color: white; }
  .v2-heading--light h2 span { color: var(--v2-lime); }
  .v2-heading--center { max-width: 960px; margin-inline: auto; text-align: center; }
  .v2-heading--center > p:last-child { max-width: 650px; margin: 20px auto 0; color: rgba(255,255,255,.62); font-size: .96rem; }
  .v2-heading--split { display: grid; grid-template-columns: minmax(0, 1.45fr) minmax(280px, .75fr); gap: 50px; align-items: end; }
  .v2-heading__aside { margin: 0; color: var(--v2-muted); font-size: .96rem; line-height: 1.65; }
  .v2-heading--light .v2-heading__aside { color: rgba(255,255,255,.68); }

  .v2-nav { position: fixed; inset: 0 0 auto; z-index: 80; height: 72px; background: rgba(6,26,28,.88); backdrop-filter: saturate(150%) blur(18px); border-bottom: 1px solid rgba(255,255,255,.08); }
  .v2-nav__inner { height: 100%; display: flex; align-items: center; gap: 34px; }
  .v2-nav__brand { width: 190px; flex: 0 0 auto; }
  .v2-nav__brand img { width: 100%; border-radius: 0; }
  .v2-nav__links { display: flex; margin-left: auto; gap: 28px; }
  .v2-nav__links a { color: rgba(255,255,255,.76); font: 500 .86rem/1 var(--font-body); }
  .v2-nav__links a:hover { color: var(--v2-lime); }

  .v2-hero { position: relative; min-height: 0; padding: 116px 0 56px; background: var(--v2-ink); color: white; }
  .v2-hero__glow { position: absolute; width: 720px; height: 720px; left: -260px; top: -260px; border-radius: 50%; background: radial-gradient(circle, rgba(186,211,5,.18), transparent 68%); pointer-events: none; }
  .v2-hero__grid { position: relative; display: grid; grid-template-columns: minmax(0, 1fr) minmax(360px, 420px); gap: 38px; align-items: center; }
  .v2-hero__message { display: grid; grid-template-columns: minmax(0, 1.08fr) minmax(230px, .72fr); gap: 24px; align-items: stretch; min-width: 0; }
  .v2-hero__copy { min-width: 0; }
  .v2-hero h1 { max-width: 560px; margin: 0; color: white; font-size: clamp(3rem, 4.25vw, 4.45rem); font-weight: 750; line-height: .94; }
  .v2-hero h1 span { display: block; color: var(--v2-lime); }
  .v2-hero__checks { display: grid; gap: 9px; margin-top: 22px; }
  .v2-hero__checks li { display: flex; align-items: flex-start; color: rgba(255,255,255,.82); font: 550 .82rem/1.42 var(--font-body); }
  .v2-hero__checks span { color: var(--v2-lime); margin-right: 5px; }
  .v2-hero__actions { display: flex; align-items: center; gap: 24px; margin-top: 22px; }
  .v2-text-link { color: white; font: 600 .94rem/1 var(--font-body); }
  .v2-text-link span { color: var(--v2-lime); margin-left: 4px; }
  .v2-hero__visual { position: relative; min-height: 390px; margin: 0; overflow: hidden; border: 1px solid rgba(255,255,255,.14); border-radius: 24px; background: var(--v2-ink-2); }
  .v2-hero__visual::after { content: ''; position: absolute; inset: 0; background: linear-gradient(180deg, transparent 42%, rgba(6,26,28,.9) 100%); }
  .v2-hero__visual img { width: 100%; height: 100%; min-height: 390px; border-radius: 0; object-fit: cover; object-position: 54% center; }
  .v2-hero__visual figcaption { position: absolute; z-index: 1; right: 18px; bottom: 18px; left: 18px; display: grid; gap: 4px; }
  .v2-hero__visual strong { color: white; font: 750 .86rem/1.25 var(--font-body); }
  .v2-hero__visual span { color: rgba(255,255,255,.64); font: 500 .72rem/1.4 var(--font-body); }
  .v2-form-shell { position: sticky; top: 90px; justify-self: end; width: 100%; max-width: 420px; padding: 10px; border: 1px solid rgba(255,255,255,.15); border-radius: 24px; background: rgba(255,255,255,.07); box-shadow: 0 22px 60px rgba(0,0,0,.3); }
  .v2-form-shell__topline { display: flex; justify-content: space-between; padding: 5px 8px 15px; color: rgba(255,255,255,.66); font: 600 .72rem/1 var(--font-body); letter-spacing: .08em; text-transform: uppercase; }
  .version2 .v2-form-shell .lead-card__body { padding: 22px !important; border-radius: 16px 16px 0 0; background: var(--v2-paper) !important; }
  .version2 .v2-form-shell .lead-card__intro { margin: 0 0 20px !important; color: var(--v2-ink) !important; font-size: 1rem !important; line-height: 1.4 !important; text-shadow: none !important; }
  .version2 .v2-form-shell .lead-card__intro br:first-child { display: none; }
  .version2 .v2-form-shell .lead-card__footer { padding: 13px 20px !important; border-radius: 0 0 16px 16px !important; background: var(--v2-ink-2) !important; }
  .version2 .v2-form-shell .lead-card__footer p { color: rgba(255,255,255,.72) !important; font-size: .78rem !important; font-weight: 500 !important; }
  .version2 .v2-form-shell .lead-form__progress-track { height: 10px !important; border-radius: 999px; background: #e1e4da !important; }
  .version2 .v2-form-shell .lead-form__progress-meter { border-radius: 999px; font-size: .72rem !important; }
  .version2 .v2-form-shell .lead-form__step.is-active { gap: 8px; }
  .version2 .v2-form-shell .lead-form__label { margin-bottom: 5px; color: var(--v2-ink) !important; font-size: .88rem !important; font-weight: 600 !important; }
  .version2 .v2-form-shell .lead-form__field,
  .version2 .v2-form-shell .lead-form__select select { height: 48px !important; border: 1px solid #dce0d6 !important; border-radius: 10px !important; background: white !important; }
  .version2 .v2-form-shell .btn-form { min-height: 48px; border-radius: 999px !important; background: var(--v2-lime) !important; color: var(--v2-ink) !important; font-weight: 700 !important; }
  .version2 .v2-form-shell .btn-form--prev { background: #d9ddd4 !important; }

  .v2-results { background: var(--v2-ink); }
  .v2-results__grid { display: grid; grid-template-columns: repeat(3, minmax(0,1fr)); gap: 14px; }
  .v2-case { padding: 10px; border: 1px solid rgba(255,255,255,.1); border-radius: 20px; background: rgba(255,255,255,.055); }
  .v2-case--wide { grid-column: auto; }
  .v2-case__images { display: grid; grid-template-columns: repeat(2,1fr); gap: 8px; }
  .v2-case figure { position: relative; margin: 0; overflow: hidden; border-radius: 14px; background: #d7d7d7; }
  .v2-case img, .v2-case--wide img { width: 100%; aspect-ratio: 1.32 / 1; object-fit: cover; border-radius: 0; }
  .v2-case figcaption { position: absolute; left: 12px; bottom: 12px; padding: 6px 10px; border-radius: 999px; background: rgba(6,26,28,.82); color: white; font: 700 .7rem/1 var(--font-body); text-transform: uppercase; letter-spacing: .08em; }
  .v2-case h3 { margin: 13px 6px 4px; color: white; font-size: .9rem; letter-spacing: -.01em; }

  .v2-method { background: var(--v2-paper); }
  .v2-method__grid { display: grid; grid-template-columns: 1fr .9fr; gap: 54px; align-items: center; }
  .v2-method__copy h2 { font-size: clamp(2.35rem, 3.5vw, 3.65rem); }
  .v2-method__lead { margin: 20px 0 22px; color: var(--v2-muted); font-size: .96rem; line-height: 1.6; }
  .v2-steps li { display: grid; grid-template-columns: 38px 1fr; gap: 14px; padding: 13px 0; border-top: 1px solid #dcdfd5; }
  .v2-steps li > span { color: #789500; font: 800 .78rem/1.5 var(--font-body); }
  .v2-steps strong { color: var(--v2-ink); font: 700 1rem/1.3 var(--font-body); }
  .v2-steps p { margin: 4px 0 0; color: var(--v2-muted); font-size: .86rem; line-height: 1.45; }
  .v2-method__gallery { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; }
  .v2-method__gallery img { width: 100%; height: 160px; border-radius: 16px; object-fit: cover; }
  .v2-method__gallery-main { grid-column: 1 / -1; height: 300px !important; }
  .v2-tech { display: grid; grid-template-columns: .8fr 1.2fr; gap: 42px; margin-top: 46px; padding-top: 38px; border-top: 1px solid #dcdfd5; }
  .v2-tech__intro h3 { max-width: 520px; color: var(--v2-ink); font-size: clamp(1.75rem, 2.5vw, 2.55rem); line-height: 1.04; }
  .v2-tech__intro > p:not(.v2-kicker) { max-width: 520px; margin: 16px 0 0; color: var(--v2-muted); font-size: .88rem; line-height: 1.55; }
  .v2-tech__cards { display: grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap: 12px; }
  .v2-tech__cards article { padding: 24px; border-radius: 20px; background: var(--v2-mint); }
  .v2-tech__cards article:nth-child(2) { background: var(--v2-ink); color: white; }
  .v2-tech__cards article > span { color: #617900; font: 750 .65rem/1 var(--font-body); letter-spacing: .1em; text-transform: uppercase; }
  .v2-tech__cards article:nth-child(2) > span { color: var(--v2-lime); }
  .v2-tech__cards h3 { margin: 11px 0 9px; color: inherit; font-size: 2.35rem; line-height: 1; }
  .v2-tech__cards p { color: var(--v2-muted); font-size: .82rem; line-height: 1.5; }
  .v2-tech__cards article:nth-child(2) p { color: rgba(255,255,255,.64); }
  .v2-tech__cards ul { margin-top: 16px; }
  .v2-tech__cards li { padding: 9px 0; border-top: 1px solid rgba(6,26,28,.16); color: inherit; font: 650 .73rem/1.35 var(--font-body); }
  .v2-tech__cards article:nth-child(2) li { border-color: rgba(255,255,255,.14); }

  .v2-media { background: var(--v2-ink); }
  .v2-media__carousel { overflow: hidden; }
  .v2-media__track { display: flex; transition: transform 420ms cubic-bezier(.22,.8,.22,1); }
  .v2-media__slide { display: grid; grid-template-columns: 1.35fr .65fr; flex: 0 0 100%; height: 360px; border: 1px solid rgba(255,255,255,.1); border-radius: 24px; overflow: hidden; opacity: .54; transition: opacity 220ms ease; }
  .v2-media__slide.is-active { opacity: 1; }
  .v2-media__video { min-width: 0; }
  .version2 .v2-media__slide .video { height: 358px; aspect-ratio: auto; border-radius: 0; }
  .version2 .v2-media .video img { border-radius: 0; object-position: center; }
  .v2-media__copy { display: flex; flex-direction: column; justify-content: flex-end; padding: 32px; background: #102b2e; }
  .v2-media__copy span { color: var(--v2-lime); font: 700 .72rem/1 var(--font-body); letter-spacing: .13em; text-transform: uppercase; }
  .v2-media__copy h3 { margin: 14px 0; color: white; font-size: clamp(1.8rem,2.5vw,2.6rem); line-height: 1; }
  .v2-media__copy p { margin: 0; color: rgba(255,255,255,.62); font-size: .92rem; line-height: 1.6; }
  .v2-media__controls { display: flex; align-items: center; justify-content: center; gap: 18px; margin-top: 18px; }
  .v2-media__controls > button { display: grid; width: 42px; height: 42px; place-items: center; border: 1px solid rgba(255,255,255,.16); border-radius: 50%; background: transparent; color: white; cursor: pointer; }
  .v2-media__dots { display: flex; gap: 7px; }
  .v2-media__dots button { width: 8px; height: 8px; padding: 0; border: 0; border-radius: 50%; background: rgba(255,255,255,.26); cursor: pointer; }
  .v2-media__dots button.is-active { width: 24px; border-radius: 999px; background: var(--v2-lime); }

  .v2-clinic { background: var(--v2-cream); }
  .v2-clinic__grid { display: grid; grid-template-columns: .85fr 1.15fr; gap: 54px; align-items: center; }
  .v2-clinic__visual { display: grid; grid-template-columns: repeat(2,1fr); gap: 12px; }
  .v2-clinic__visual img { width: 100%; border-radius: 22px; background: var(--v2-ink); }
  .v2-clinic__visual img:nth-child(2) { margin-top: 30px; }
  .v2-clinic__copy > p:not(.v2-kicker) { margin: 20px 0 22px; color: var(--v2-muted); font-size: .94rem; line-height: 1.6; }
  .v2-clinic__doctors > div { display: grid; padding: 12px 0; border-top: 1px solid #d9dccf; }
  .v2-clinic__doctors strong { color: var(--v2-ink); font: 700 .95rem/1.35 var(--font-body); }
  .v2-clinic__doctors span { color: var(--v2-muted); font: 500 .8rem/1.4 var(--font-body); }
  .v2-faq { background: var(--v2-paper); }
  .v2-faq__grid { display: grid; grid-template-columns: .72fr 1.28fr; gap: 70px; align-items: start; }
  .v2-faq__intro { position: sticky; top: 110px; }
  .v2-faq__intro h2 { font-size: clamp(2.35rem,3.6vw,3.75rem); }
  .v2-faq__intro img { width: 100%; max-width: 410px; margin-top: 24px; aspect-ratio: 1.55 / 1; border-radius: 18px; object-fit: cover; object-position: center 24%; }
  .v2-faq__list details { border-top: 1px solid #d9dccf; }
  .v2-faq__list details:last-child { border-bottom: 1px solid #d9dccf; }
  .v2-faq__list summary { display: flex; min-height: 68px; align-items: center; justify-content: space-between; gap: 20px; cursor: pointer; list-style: none; color: var(--v2-ink); font: 700 .96rem/1.35 var(--font-body); }
  .v2-faq__list summary::-webkit-details-marker { display: none; }
  .v2-faq__list summary span { font-size: 1.5rem; transition: rotate 180ms ease; }
  .v2-faq__list details[open] summary span { rotate: 45deg; }
  .v2-faq__list details p { max-width: 700px; margin: -2px 46px 20px 0; color: var(--v2-muted); font-size: .88rem; line-height: 1.6; }

  .v2-final { padding: 0 0 18px; background: var(--v2-paper); }
  .v2-final__panel { display: grid; grid-template-columns: minmax(260px,.72fr) minmax(0,1.28fr); min-height: 310px; padding: 12px; border-radius: 24px; background: var(--v2-ink); }
  .v2-final__panel > img { width: 100%; height: 100%; min-height: 286px; border-radius: 16px; object-fit: cover; object-position: center 28%; }
  .v2-final__copy { display: flex; flex-direction: column; align-items: flex-start; justify-content: center; padding: 34px 42px; }
  .v2-final h2 { max-width: 700px; color: white; font-size: clamp(2.4rem,3.7vw,3.65rem); }
  .v2-final__copy .v2-button { margin-top: 26px; }
  .v2-footer { padding: 48px 0 100px; background: var(--v2-ink); color: white; }
  .v2-footer__inner { display: grid; grid-template-columns: 1fr auto auto; gap: 60px; align-items: center; padding-top: 34px; border-top: 1px solid rgba(255,255,255,.12); }
  .v2-footer img { width: 190px; border-radius: 0; }
  .v2-footer__inner > div { display: grid; }
  .v2-footer strong { font: 700 .82rem/1.4 var(--font-body); }
  .v2-footer span { color: rgba(255,255,255,.55); font: 500 .74rem/1.4 var(--font-body); }
  .v2-floating { position: fixed; z-index: 70; right: 22px; bottom: 20px; display: flex; align-items: center; gap: 18px; padding: 15px 18px 15px 22px; border: 1px solid rgba(255,255,255,.15); border-radius: 999px; background: var(--v2-lime); box-shadow: 0 18px 50px rgba(6,26,28,.3); color: var(--v2-ink); font: 800 .86rem/1 var(--font-body); opacity: 0; pointer-events: none; translate: 0 18px; transition: opacity 180ms ease, translate 180ms ease; }
  .v2-floating span { display: grid; width: 28px; height: 28px; place-items: center; border-radius: 50%; background: var(--v2-ink); color: white; }
  .v2-floating.is-visible { opacity: 1; pointer-events: auto; translate: 0; }
  .v2-form-shell.is-emphasized { animation: v2-form-pulse 760ms cubic-bezier(.2,.8,.2,1); }
  @keyframes v2-form-pulse { 0%,100% { transform: scale(1); } 34% { transform: scale(1.025); box-shadow: 0 0 0 8px rgba(186,211,5,.18), 0 28px 80px rgba(0,0,0,.35); } }

  @media (max-width: 1023px) {
    .v2-nav__links { display: none; }
    .v2-nav__cta { margin-left: auto; }
    .v2-hero { min-height: 0; }
    .v2-hero__grid { grid-template-columns: minmax(0,1fr) 380px; gap: 28px; }
    .v2-hero__message { grid-template-columns: 1fr; }
    .v2-hero h1 { font-size: clamp(3.3rem,6.5vw,4.7rem); }
    .v2-hero__visual { min-height: 210px; }
    .v2-hero__visual img { min-height: 210px; }
    .v2-heading--split { gap: 40px; }
    .v2-method__grid, .v2-clinic__grid { gap: 42px; }
    .v2-faq__grid { gap: 60px; }
  }

  @media (max-width: 767px) {
    html { scroll-padding-top: 68px; }
    .v2-container { width: min(100% - 28px, 680px); }
    .v2-section { padding: 54px 0; }
    .v2-nav { height: 64px; }
    .v2-nav__brand { width: 154px; }
    .v2-nav__cta { min-height: 40px; padding: 10px 15px; font-size: .78rem; }
    .v2-hero { padding: 92px 0 36px; }
    .v2-hero__grid { display: block; }
    .v2-hero h1 { font-size: clamp(2.75rem,12.8vw,3.6rem); line-height: .94; }
    .v2-hero__checks { display: grid; gap: 7px; }
    .v2-hero__actions { margin: 20px 0 28px; }
    .v2-hero__visual { height: 220px; min-height: 0; margin-bottom: 14px; }
    .v2-hero__visual img { height: 220px; min-height: 0; object-position: center 40%; }
    .v2-form-shell { position: relative; top: auto; padding: 8px; border-radius: 22px; }
    .version2 .v2-form-shell .lead-card__body { padding: 20px !important; }
    .version2 .v2-form-shell .lead-card__intro { font-size: 1rem !important; }
    .v2-heading { margin-bottom: 28px; }
    .v2-heading--split { display: block; }
    .v2-heading--split .v2-heading__aside { margin-top: 18px; }
    .v2-heading h2, .version2 h2 { font-size: clamp(2.2rem,9.8vw,3.05rem); }
    .v2-results__grid { display: grid; grid-auto-flow: column; grid-auto-columns: 90%; grid-template-columns: none; gap: 12px; margin-right: -14px; padding-right: 14px; overflow-x: auto; scroll-snap-type: x mandatory; scrollbar-width: none; }
    .v2-results__grid::-webkit-scrollbar { display: none; }
    .v2-case { margin-bottom: 0; scroll-snap-align: start; }
    .v2-case--wide { grid-column: auto; }
    .v2-case img, .v2-case--wide img { aspect-ratio: 1.32 / 1; }
    .v2-method__grid, .v2-clinic__grid, .v2-faq__grid { grid-template-columns: 1fr; gap: 30px; }
    .v2-method__gallery-main { height: 210px !important; }
    .v2-method__gallery img { height: 120px; }
    .v2-tech { grid-template-columns: 1fr; gap: 22px; margin-top: 36px; padding-top: 32px; }
    .v2-tech__intro h3 { font-size: 2rem; }
    .v2-tech__cards { gap: 9px; }
    .v2-tech__cards article { padding: 20px 16px; }
    .v2-tech__cards h3 { font-size: 2rem; }
    .v2-media__slide { display: block; height: auto; }
    .v2-media__video { aspect-ratio: 16 / 9; }
    .version2 .v2-media__slide .video { height: 100%; aspect-ratio: 16 / 9; }
    .v2-media__copy { padding: 24px 20px; }
    .v2-clinic__visual img:nth-child(2) { margin-top: 20px; }
    .v2-faq__intro { position: static; }
    .v2-faq__list summary { min-height: 64px; font-size: .88rem; }
    .v2-final__panel { grid-template-columns: 1fr; min-height: 0; padding: 9px; }
    .v2-final__panel > img { min-height: 220px; max-height: 270px; }
    .v2-final__copy { padding: 26px 16px 28px; }
    .v2-footer { padding-bottom: 104px; }
    .v2-footer__inner { grid-template-columns: 1fr; gap: 24px; }
    .v2-floating { right: 12px; bottom: 10px; left: 12px; justify-content: space-between; }
  }

  @media (prefers-reduced-motion: reduce) {
    .v2-button, .v2-floating, .v2-form-shell { transition: none; animation: none !important; }
  }
</style>

<script>
  const formShell = document.querySelector<HTMLElement>('[data-form-shell]');
  const floating = document.querySelector<HTMLElement>('[data-floating]');
  let pulseTimer: number | undefined;

  const emphasizeForm = () => {
    if (!formShell) return;
    window.clearTimeout(pulseTimer);
    formShell.classList.remove('is-emphasized');
    void formShell.offsetWidth;
    formShell.classList.add('is-emphasized');
    pulseTimer = window.setTimeout(() => formShell.classList.remove('is-emphasized'), 900);
  };

  if (formShell && floating && 'IntersectionObserver' in window) {
    const observer = new IntersectionObserver(
      ([entry]) => floating.classList.toggle('is-visible', !entry.isIntersecting),
      { threshold: 0.08 }
    );
    observer.observe(formShell);
  }

  document.querySelectorAll<HTMLAnchorElement>('.version2 a[href="#form"]').forEach((link) => {
    link.addEventListener('click', (event) => {
      if (!formShell) return;
      const rect = formShell.getBoundingClientRect();
      const visible = rect.top < innerHeight && rect.bottom > 64;
      if (visible) {
        event.preventDefault();
        emphasizeForm();
      }
    });
  });

  const mediaTrack = document.querySelector<HTMLElement>('[data-media-track]');
  const mediaSlides = [...document.querySelectorAll<HTMLElement>('[data-media-slide]')];
  const mediaDots = [...document.querySelectorAll<HTMLButtonElement>('[data-media-dot]')];
  let activeMedia = 0;

  const showMedia = (index: number) => {
    if (!mediaTrack || mediaSlides.length === 0) return;
    activeMedia = (index + mediaSlides.length) % mediaSlides.length;
    mediaTrack.style.transform = `translateX(-${activeMedia * 100}%)`;
    mediaSlides.forEach((slide, slideIndex) => slide.classList.toggle('is-active', slideIndex === activeMedia));
    mediaDots.forEach((dot, dotIndex) => {
      const active = dotIndex === activeMedia;
      dot.classList.toggle('is-active', active);
      dot.setAttribute('aria-current', active ? 'true' : 'false');
    });
  };

  document.querySelector<HTMLButtonElement>('[data-media-prev]')?.addEventListener('click', () => showMedia(activeMedia - 1));
  document.querySelector<HTMLButtonElement>('[data-media-next]')?.addEventListener('click', () => showMedia(activeMedia + 1));
  mediaDots.forEach((dot) => dot.addEventListener('click', () => showMedia(Number(dot.dataset.mediaDot || 0))));
</script>
